#include <stdio.h>

// types of variables
// integers (positive or negative numbers with no decimals) :
// char, int, shot, long, long long, unsigned short, unsigned long, unsigned long long
// floating point numbers (numbers with decimals) :
// float, double
// structures (multiple variables into one)

// operations you can use to add, multiply or divide variables :
// multiplication : *
// division : /
// addition : +
// substraction : -
// modulo : %
// assignment : =

// you can use each of these operators while assigning a variable
// instead of writing :
// int a = 5;
// a = a + 5;
// i could write :
// int a = 5;
// a += 5

int main() {
	// declare a variable of type int named "a" and set its value to 3
	// declare a variable of type float named "b" and set its value to 4.5
	// declare a variable of type double named "c" and set its value to 5.25
	// declare a variable of type int named "sum" and don't set its value yet
	
	// set value of sum to the sum of a, b and c

	printf("The sum of a, b and c is %f.\n", sum);
	return (0);
}
